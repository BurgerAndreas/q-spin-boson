from enum import Enum

class Paras(Enum):
    # Epsilon, Omega, Lambda
    # Values are taken from the paper
    SB1S = [.5, 4, 2]
    SB2S = [.5, 6, 2]
    # Values are taken from the master thesis
    # JC1S = [1, 1, 1]
    # JC2S = [2, 2, 2]